package com.ironmind.app.domain.model

/** The stated goal for an AI-generated routine — shapes the sets/reps range the model targets. */
enum class TrainingGoal {
    STRENGTH,
    HYPERTROPHY,
    ENDURANCE,
}
